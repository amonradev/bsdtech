import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
    TextTop: {
        color: "#fff",
        fontSize: 18
    },
    TopContainer: {
        margin: 30,
        flexDirection: "row"
    },
    ButtonContainer: {
        flexDirection: "row"
    }
})